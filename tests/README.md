# Tests

This directory is reserved for cross-cutting scenarios that should not live inside a single app or module.

Expected future coverage areas:

- RBAC scenario matrices
- parent-child access checks
- webhook idempotency fixtures
- billing lifecycle fixtures
- migration smoke tests
